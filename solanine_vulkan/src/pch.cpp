#include "pch.h"

// @CHECK: do we even need there to be a .cpp file that is used to create the pch? Why can't pch.h be the create file?
// @ANSWER: you can't set this settings on a .h file bc it never gets compiled.... it's just the design of the compiler eh!
